version: "3.9"

services:

  # ── Sentinel OverWatch — Rust Governance Spine ────────────────────────────
  sentinel:
    build:
      context: ./governance-spine
      dockerfile: Dockerfile
    container_name: asf-sentinel
    ports:
      - "9090:8080"
    environment:
      - RUST_LOG=info
      - SENTOW_INDUSTRY_PROFILE=${SENTOW_INDUSTRY_PROFILE:-consumer}
      - SENTOW_HAAP_DRS_CEILING=${SENTOW_HAAP_DRS_CEILING:-60}
      - SENTOW_MEMORY_PATH=/app/audit/strategic_memory.json
    volumes:
      - sentinel-audit:/app/audit
      - sentinel-data:/data
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/health"]
      interval: 15s
      timeout: 5s
      retries: 5

  # ── Abigail — Constitutional Control Plane (CP-00) ────────────────────────
  abby:
    build:
      context: ./abigail
      dockerfile: Dockerfile
    container_name: asf-abby
    ports:
      - "7070:7070"
    volumes:
      - ./.abigail.env:/app/.abigail.env:ro
      - abigail-logs:/app/logs
    environment:
      - ABIGAIL_BACKEND=${ABIGAIL_BACKEND:-groq}
      - ABIGAIL_HEADLESS=1
      - SENTINEL_URL=http://sentinel:8080
      - ABIGAIL_ADMIN_TOKEN=${ABIGAIL_ADMIN_TOKEN}
      - ABIGAIL_DEMO_TOKEN=${ABIGAIL_DEMO_TOKEN}
    depends_on:
      sentinel:
        condition: service_healthy
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "python3", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:9090/api/status', timeout=5)"]
      interval: 30s
      timeout: 10s
      retries: 3

volumes:
  sentinel-audit:
  sentinel-data:
  abigail-logs:

networks:
  default:
    name: asf-network
    driver: bridge

